## Plan: Apply 14 missing migrations in dependency-safe order

**Pre-check confirmed:** DB has the expected 8 tables and all existing data is intact (8 auditions, 11 takes, 1 app_config, 5 take_qa_traces, 4 email tables). No reset, no rollback, no data loss.

### Execution order (as specified — dependency-safe, NOT timestamp-sorted)

1. `20260527071000_account_route_consent_gate`
2. `20260527093200_consumer_credit_products`
3. `20260527094500_credit_ledger_foundation`
4. `20260527113200_partner_entities_codes_memberships`
5. `20260527115600_partner_credit_pools_caps_alerts` *(pulled ahead of package presets — pools/caps are a dependency)*
6. `20260527121500_partner_visibility_dashboard_foundation` *(pulled ahead of package presets)*
7. `20260527123200_school_partner_package_presets`
8. `20260527114600_coach_partner_package_presets`
9. `20260527120200_agent_partner_package_presets`
10. `20260527130500_report_credit_reservation_lifecycle`
11. `20260527133500_consumer_stripe_checkout_webhooks`
12. `20260527143800_ai_usage_cost_baseline`
13. `20260527150600_cfo_margin_dashboards`
14. `20260527165000_analytics_attribution_habit_tracking`

### Execution rules

- Submit each migration as a separate `supabase--migration` call, in the order above, using the file's SQL verbatim from `supabase/migrations/`.
- Wait for Supabase to report success on each migration before submitting the next.
- **Do not modify the SQL** of any migration to work around an error.
- **Do not manually patch `src/integrations/supabase/types.ts`** at any point. Types are regenerated by Supabase automatically after all 14 migrations complete.

### Failure handling

If any migration fails — including "object already exists" / "already applied" / any other Postgres error:

1. **Stop immediately.** Do not attempt the next migration.
2. Report the exact failing migration filename, the exact SQL statement that failed, and the verbatim Postgres error message.
3. Do not retry, edit, or skip past the failure without explicit direction from the user.

The only exception is if Supabase itself reports the migration succeeded as a no-op for a genuinely idempotent migration — in which case continue.

### After all 14 succeed

1. Let Supabase auto-regenerate `src/integrations/supabase/types.ts`.
2. Rerun the build. Investigate any specific TS errors individually — no blanket `as any` casts.
3. Once the build is green, re-run email infrastructure setup to refresh the Vault secret and activate the queue dispatcher cron.

### Will NOT do

- Reset, restore, or roll back the DB.
- Roll back code.
- Cast missing references to `any`.
- Manually edit `types.ts` mid-sequence.
- Edit migration SQL to bypass failures.
- Retry email cron activation until the build is green.
